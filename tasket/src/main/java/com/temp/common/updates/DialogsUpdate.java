package com.temp.common.updates;

import com.temp.model.models.Dialog;

public class DialogsUpdate extends Update<Dialog> {
    public DialogsUpdate(Dialog update) {
        super(update);
    }
}
