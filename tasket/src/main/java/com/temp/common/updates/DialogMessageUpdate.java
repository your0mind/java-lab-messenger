package com.temp.common.updates;

import com.temp.common.models.ChatMessage;

public class DialogMessageUpdate extends Update<ChatMessage> {
    public DialogMessageUpdate(ChatMessage update) {
        super(update);
    }
}
