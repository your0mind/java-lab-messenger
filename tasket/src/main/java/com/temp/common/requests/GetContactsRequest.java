package com.temp.common.requests;

import com.temp.common.requests.params.GetContactsRequestParams;

public class GetContactsRequest extends Request<GetContactsRequestParams> {
    public GetContactsRequest(GetContactsRequestParams params) {
        super(params);
    }
}
