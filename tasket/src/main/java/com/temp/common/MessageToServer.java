package com.temp.common;

import com.temp.common.requests.RequestInfo;

public class MessageToServer {
    public RequestInfo requestInfo;

    public MessageToServer(RequestInfo requestInfo) {
        this.requestInfo = requestInfo;
    }
}
